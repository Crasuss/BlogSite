@model IEnumerable<web1.Models.Yorum>

@{
    Layout = null;
}
<div class="card my-4">
<h5 class="card-header">Son Yorumlar</h5>
<div class="card-body">
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Icerik</li>
        }
    </ul>
</div>

</div>